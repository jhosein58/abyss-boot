
fn entry {


    let a: &i64 = malloc(size(i64) * 100) as &i64;

    for i in 0 -> 100 {
        a[i] = i
    }

    for i in 0 -> 100 {
        printf("%f ", a[i]);
    }

    printf("\n")

    free(a);

}
